# Standard Library Documentation

This directory contains prelude and standard-library design material.

- `adr/`: decisions relevant to unsafe builtins, intrinsic use, runtime plugin
  boundaries, and contextual operation values.
- `contexts/`: glossary slices relevant to prelude and contextual resolution.

Language rules belong in `spec`; compiler implementation details belong in
`lanec`.
