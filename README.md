# Lane Standard Library

This directory is reserved for the Lane standard library source.

The first standard library surface is the prelude described in `docs/prelude-v1.md`; implementation files will be added here once the compiler can parse and check Lane source.

